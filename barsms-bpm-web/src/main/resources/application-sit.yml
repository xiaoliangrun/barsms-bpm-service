server:
  port: 8080
  servlet:
    context-path: /api/bpm

spring:
  application:
    name: barsms-bpm-service
  redis:
    host: 127.0.0.1
    port: 6379
    password: rxl13456
    database: 0
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://myhome.mysql.polardb.rds.aliyuncs.com:3306/barsmsbpm?rewriteBatchedStatements=true&allowMultiQueries=true&useLocalSessionState=true&useUnicode=true&characterEncoding=utf-8&enableQueryTimeouts=false
      username: rxl13456
      password: uD(MC3IGzHdv
      initialSize: 5
      minIdle: 5
      maxActive: 30
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1
      keepAlive: true
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      filters: stat,wall,slf4j,config
      useGlobalDataSourceStat: true
      stat:
        log-slow-sql: true
        merge-sql: true
        slow-sql-millis: 10000

mybatis-plus:
  mapper-locations: classpath*:/sqlmapper/*.xml
  type-aliases-package: com.cpic.barsms.bpm.infra.model.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
